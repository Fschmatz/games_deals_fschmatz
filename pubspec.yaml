name: games_deals_fschmatz
description: Flutter+Dart
publish_to: 'none'
version: 1.7.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider:
  shared_preferences:
  provider:
  url_launcher:
  webfeed:
  http:
  dynamic_value:
  share:
  easy_dynamic_theme:
  flutter_lints: ^1.0.0
  cupertino_icons: ^1.0.2
  jiffy:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^1.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
  fonts:
    - family: StoreIcons
      fonts:
        - asset: assets/StoreIcons.ttf
